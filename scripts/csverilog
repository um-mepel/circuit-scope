#!/usr/bin/env bash
# Delegate to run-csverilog.mjs (same entry point as npm / Windows csverilog.cmd).
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "$SCRIPT_DIR/run-csverilog.mjs" "$@"
